export class User{
  username: string;
  id: number;
  email: string;

  constructor(id: number, username: string, email: string) {
    this.id = id;
    this.username = name;
    this.email = email;
  }
}
